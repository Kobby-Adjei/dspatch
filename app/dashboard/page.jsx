import CommandCenter from "./CommandCenter";

export const metadata = {
  title: "Command Center — DSPatch",
  description: "Live operational dashboard.",
};

export default function DashboardPage() {
  return <CommandCenter />;
}
