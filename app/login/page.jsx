import LoginForm from "./LoginForm";

export const metadata = {
  title: "Log in — DSPatch",
  description: "Access your DSPatch command center.",
};

export default function LoginPage() {
  return <LoginForm />;
}
